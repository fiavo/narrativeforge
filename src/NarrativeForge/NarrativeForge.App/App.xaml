<Application x:Class="NarrativeForge.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <local:InvertBoolConverter x:Key="InvertBool"
                xmlns:local="clr-namespace:NarrativeForge.App"/>

            <!-- Catppuccin Mocha Theme -->
            <Color x:Key="BaseColor">#1e1e2e</Color>
            <Color x:Key="MantleColor">#181825</Color>
            <Color x:Key="CrustColor">#11111b</Color>
            <Color x:Key="Surface0Color">#313244</Color>
            <Color x:Key="Surface1Color">#45475a</Color>
            <Color x:Key="Surface2Color">#585b70</Color>
            <Color x:Key="Overlay0Color">#6c7086</Color>
            <Color x:Key="Overlay1Color">#7f849c</Color>
            <Color x:Key="Subtext0Color">#a6adc8</Color>
            <Color x:Key="Subtext1Color">#bac2de</Color>
            <Color x:Key="TextColor">#cdd6f4</Color>
            <Color x:Key="LavenderColor">#b4befe</Color>
            <Color x:Key="BlueColor">#89b4fa</Color>
            <Color x:Key="SapphireColor">#74c7ec</Color>
            <Color x:Key="SkyColor">#89dceb</Color>
            <Color x:Key="TealColor">#a6e3a1</Color>
            <Color x:Key="GreenColor">#a6e3a1</Color>
            <Color x:Key="YellowColor">#f9e2af</Color>
            <Color x:Key="PeachColor">#fab387</Color>
            <Color x:Key="MaroonColor">#eba0ac</Color>
            <Color x:Key="RedColor">#f38ba8</Color>
            <Color x:Key="MauveColor">#cba6f7</Color>
            <Color x:Key="PinkColor">#f5c2e7</Color>
            <Color x:Key="FlamingoColor">#f2cdcd</Color>
            <Color x:Key="RosewaterColor">#f5e0dc</Color>

            <SolidColorBrush x:Key="Base" Color="{StaticResource BaseColor}"/>
            <SolidColorBrush x:Key="Mantle" Color="{StaticResource MantleColor}"/>
            <SolidColorBrush x:Key="Crust" Color="{StaticResource CrustColor}"/>
            <SolidColorBrush x:Key="Surface0" Color="{StaticResource Surface0Color}"/>
            <SolidColorBrush x:Key="Surface1" Color="{StaticResource Surface1Color}"/>
            <SolidColorBrush x:Key="Surface2" Color="{StaticResource Surface2Color}"/>
            <SolidColorBrush x:Key="Overlay0" Color="{StaticResource Overlay0Color}"/>
            <SolidColorBrush x:Key="Overlay1" Color="{StaticResource Overlay1Color}"/>
            <SolidColorBrush x:Key="Subtext0" Color="{StaticResource Subtext0Color}"/>
            <SolidColorBrush x:Key="Subtext1" Color="{StaticResource Subtext1Color}"/>
            <SolidColorBrush x:Key="Text" Color="{StaticResource TextColor}"/>
            <SolidColorBrush x:Key="Lavender" Color="{StaticResource LavenderColor}"/>
            <SolidColorBrush x:Key="Blue" Color="{StaticResource BlueColor}"/>
            <SolidColorBrush x:Key="Sapphire" Color="{StaticResource SapphireColor}"/>
            <SolidColorBrush x:Key="Sky" Color="{StaticResource SkyColor}"/>
            <SolidColorBrush x:Key="Teal" Color="{StaticResource TealColor}"/>
            <SolidColorBrush x:Key="Green" Color="{StaticResource GreenColor}"/>
            <SolidColorBrush x:Key="Yellow" Color="{StaticResource YellowColor}"/>
            <SolidColorBrush x:Key="Peach" Color="{StaticResource PeachColor}"/>
            <SolidColorBrush x:Key="Maroon" Color="{StaticResource MaroonColor}"/>
            <SolidColorBrush x:Key="Red" Color="{StaticResource RedColor}"/>
            <SolidColorBrush x:Key="Mauve" Color="{StaticResource MauveColor}"/>
            <SolidColorBrush x:Key="Pink" Color="{StaticResource PinkColor}"/>
            <SolidColorBrush x:Key="Flamingo" Color="{StaticResource FlamingoColor}"/>
            <SolidColorBrush x:Key="Rosewater" Color="{StaticResource RosewaterColor}"/>

            <!-- Global Button Style -->
            <Style TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource Surface1}"/>
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource Surface2}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource Overlay0}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- TextBox Style -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{DynamicResource Surface0}"/>
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                <Setter Property="CaretBrush" Value="{DynamicResource Text}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Surface2}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,6"/>
            </Style>

            <!-- ListBox Style -->
            <Style TargetType="ListBox">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>

            <Style TargetType="ListBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="2">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource Surface0}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource Surface1}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Slider Style -->
            <Style TargetType="Slider">
                <Setter Property="Foreground" Value="{DynamicResource Mauve}"/>
                <Setter Property="Background" Value="{DynamicResource Surface0}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
